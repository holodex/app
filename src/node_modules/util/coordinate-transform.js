module.exports = function (options) {
  var point = options.DOMNode.createSVGPoint()
  point.x = options.coords[0]
  point.y = options.coords[1]

  //  convert mouse coordinates to svg coordinates
  return point.matrixTransform(options.layer.getCTM().inverse())
}
