module.exports = function (Model, Collection) {
  var collectionName = Model.prototype.collectionName
  var indices = Object.keys(Model.prototype._definition || {})

  return new Collection(collectionName, {
    indices: indices
  })
}

