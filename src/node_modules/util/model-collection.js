var store = require('base/store')

module.exports = function (Model, Collection) {
  return new Collection(store, {
    filter: function (model) {
      return model.getType() === Model.prototype.getType()
    },
  })
}

