var React = require('react')
var r = require('r-dom')
var Paper = require('material-ui/lib/paper')
var debug = require('debug')('legend-widget')

module.exports = React.createClass({

  contextTypes: {
    muiTheme: React.PropTypes.any
  },

  propTypes: {
    legendItems: React.PropTypes.array
  },

  render: function () {
    debug('render legend-widget', this.props)

    return (
      r(Paper, {
        zDepth: 1,
        className: 'legend-widget',
        style: {
          position: 'absolute',
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          flexDirection: 'column',
          bottom: this.context.muiTheme.spacing.desktopGutter
        }
      },
      this.props.legendItems)
    )
  }
})
