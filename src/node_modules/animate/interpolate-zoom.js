var interpolate = require('util/interpolate')

module.exports = function(p0, p1) {
  var iX = interpolate(p0[0], p1[0])
  var iY = interpolate(p0[1], p1[1])
  var iW = interpolate(p0[2], p1[2])

  return function (t) {
    return [iX(t), iY(t), iW(t)]
  }
}
