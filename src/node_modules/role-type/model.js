var Model = require('base/model')
var debug = require('debug')("holodex:role-type:model")

module.exports = Model.extend({
  typeName: "RoleType",
  //pluralName: "rolesTypes",

  props: {
    name: "string",
    pluralName: "string",
    label: "string"
  }
})
