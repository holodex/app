const flux = require('flux')

const actionTypes = require('./action-types')
const getters = require('./getters')

module.exports = {
  fetchAgents
}

function fetchAgents () {
  reactor.dispatch(actionTypes.RECEIVE_AGENTS, {
    mikey: {
      name: "Mikey"
    }
  })
}
