import { GET_AGENT } from './actions'
import { agentById } from './getters'



function agents (state = initialState, action) {
  switch (action.type) {
  case GET_AGENT:
    return agentsById[action.payload.id]
  }
}
