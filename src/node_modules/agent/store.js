var store = require('base/store')
var Collection = require('./collection')

module.exports = new Collection(store, {
  filter: function (model) {
    var type = model.getType()
    return (
      type === "Agent" ||
      type === "Person" ||
      type === "Group"
    )
  },
})
