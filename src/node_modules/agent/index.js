'use strict';

const flux = require('flux')

flux.registerStores({
  agents: require('./stores/agents')
})

module.exports = {
  vocab: require('./vocab'),
  actions: require('./actions'),
  getters: require('./getters')
}
