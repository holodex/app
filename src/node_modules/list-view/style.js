var vars = require('ui/vars')

module.exports = {

  listItem: {
    paddingLeft: vars.gutterWidth / 2,
  },

  listItemImage: {
    width: 32,
    height: 32,
    borderRadius: 3,
  },

  listItemText: {
    paddingLeft: vars.gutterWidth / 2,
  },
}
