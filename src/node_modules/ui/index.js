import { crateStructuredSelector } from 'reselect'
import component from 'virtual-component'
const debug = require('debug')('ui:index')
const Graph = require('graph')
const extend = require('xtend')
import element from 'virtex-element'
// import  { getProps } from 'getters'

console.log('Graph', Graph, component)

const shouldUpdate = () => {
  debug('shouldUpdate')
  return true
}

function render (state) {
  debug( 'render', state)
  return element(Graph, { ...state, route: 'simon' })
}

const Ui = {
  render,
  shouldUpdate
}

// module.exports = (props, children) => component(Ui, props, children)
export default render

//getters
// import getters from 'getters'
// const allProps = (state) => {
//   return {
//     state,
//     getters: getters
//   }
// }
//
// const Ui = React.createClass({
//
//   // childContextTypes: {
//   //   muiTheme: React.PropTypes.object
//   // },
//
//   // getChildContext: function () {
//   //   return {
//   //     muiTheme: themeManager.getCurrentTheme()
//   //   }
//   // },
//
//   render: function () {
//
//
//     debug('props, state', this.props, this.state)
//     const { props } = this.props
//
//     return r(Graph, {
//       ...props,
//       route: { context: 'simon' }
//     })
//
//   },
//
// })
//
//
//
// export default connect(allProps)(Ui)
