import { createSelector } from 'reselect'
import filter from 'lodash.filter'
import { agentQueries } from 'query/getters'

const initialState = require('./reducers').initialState

// TEMP
const state = { queryDesc: initialState }
const queryDescById = state => state.queryDesc.byId

// const relQueryDesc = (relId, agentType) => {
//   return createSelector(
//     [queryDescById],
//     queryTextById => {
//       if (agentType) {
//         return filter(queryDescById, (label) => {
//           return label.relationship === relId && label.agentType === agentType
//         })
//       } else {
//         return filter(queryDescById, (label) => {
//           return label.relationship === relId
//         })
//       }
//     }
//   )
// }

// const agentQueryDesc = (agentId) => {
//   return createSelector(
//     [agentQueries(agentId), queryDescById],
//     (agentQueries, queryDescById) => {
//
//     }
//
//   )
// }

module.exports = {
  queryDescById: queryDescById,
  relQueryDesc: relQueryDesc,
}
