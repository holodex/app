var extend = require('xtend/mutable');
var inherits = require('inherits');
var Agent = require('agent/model');

function Person () {
  Agent.apply(this, arguments);
}

module.exports = Person;

inherits(Person, Agent);

extend(Person.prototype, {
  
  modelType: "Person",

});
