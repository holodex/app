//place context link queries on top of direct link queries

module.exports = function (queryA, queryB) {
  if (queryA.context) {
    if (queryB.context) { return 0 }
    else { return 1 }
  }
  else { return 0 }
}
