var React = require('react');
// var h = require('create-element');
var debug = require('debug')("node-ui");

// h.selfClosingTags.image = true;

module.exports = React.createClass({
	render: function () {
		var d = this.props.datum;
		var rect = this.props.rect;

		var w = 40,
				h = 40,
				offset = w/2;

		var x = d.x  + rect.left - offset,
				y = d.y  + rect.top - offset;

		// var img = h.attributes(
		// 	{ 
		// 		'xlink:href': d.image,
		// 		height: h-offset,
		// 		width: w-offset,
		// 		x: d.x,
		// 		y: d.y
		// 	}
		// )

		return React.createElement("img",
			{
				src: d.image,
				height: h,
				width: w,
				style: {
					top: y,
					left: x
				},
				className: 'node circle-clip-20'
			}

			// , 
			// {
			// 	dangerouslySetInnerHTML: {
			// 		__html: '<image' + img + ' />'
			// 	}
			// }


			// // {
			// // 	r: 5,
			// // 	cx: d.x,
			// // 	cy: d.y,
			// // 	fill: 'grey'
			// // }
		);
	}
}) 