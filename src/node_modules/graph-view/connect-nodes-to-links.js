var map = require('lodash.map')

module.exports = function (nodes, links) {
  return map(links, function (link) {
    if (typeof link.source === 'number') {
      link.source = nodes[link.source]
      link.target = nodes[link.target]
    }
    return link
  })
}
