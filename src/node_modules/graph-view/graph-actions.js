module.exports = {
  // not used
  toggleRelationship: function (rel) {
    var cmp = this
    var relationshipObj = cmp.state.relationshipObj
    relationshipObj[rel] = !relationshipObj[rel]
    cmp.setState({relationshipObj: relationshipObj })
  }
}
