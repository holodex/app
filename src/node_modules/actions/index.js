// canonical list of UI actions
var Reflux = require('reflux')
var debug = require('debug')('actions:index')

var Actions = Reflux.createActions([
  'addNodesAndLinks',
  'updateFilters',
  'updateTag'
])

debug('ACTIONS', Actions)

module.exports = Actions
