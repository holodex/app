var ObservCollection = require('observ-node-array')

module.exports = function ObservGraph (opts) {

  // default options
  opts.models = opts.models || {}
  opts.typeAttribute = opts.typeAttribute || 'type'

  return ObservCollection({
    nodes: opts.models,
    nodeKey: opts.typeAttribute,
  })
}
