const rolesByRelationship = [
  ['relationships'],
  ['roles'],
  function (rels, rols) {
    return rels.map(function (rel, relId) {
      return rols.filter(function (rol) {
        return rol.relationship === relId
      })
    })
  }
]

module.exports = {
  rolesByRelationship
}
