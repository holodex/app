var extend = require('xtend/mutable');
var Collection = require('base/collection');

var Relationships = Collection.extend({});

module.exports = Relationships;

extend(Relationships.prototype, {
  model: require('./model'),
});
