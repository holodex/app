'use strict';

const flux = require('flux')

const actionTypes = require('./action-types')
const getters = require('./getters')

module.exports = {
  fetchRelationships
}

function fetchRelationships () {
  reactor.dispatch(actionTypes.RECEIVE_RELATIONSHIPS, {
    '0': {
      relationshipType: 'mentors',
      source: 'simon',
      target: 'mikey'
    }
  })
}
