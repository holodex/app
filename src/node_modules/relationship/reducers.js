import testState from 'state'

function relationshipReducer (state = testState, action) {
  if (typeof state === 'undefined') {
    return testState
  }

  return state
}

export default relationshipReducer
