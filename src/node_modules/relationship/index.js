'use strict';

const flux = require('flux')

flux.registerStores({
  relationships: require('./stores/relationships')
})

module.exports = {
  vocab: require('./vocab'),
  actions: require('./actions'),
  getters: require('./getters')
}
