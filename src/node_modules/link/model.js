// var debug = require('debug')('holodex:link-type:model')

var Model = require('base/model')

module.exports = Model.extend({
  typeName: 'Link',
  collectionName: 'roles',

  props: {
    type: 'link',
    agent: 'link',
    relationship: 'link'
  },
})
