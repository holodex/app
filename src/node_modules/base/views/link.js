var Link = require('link')
var router = require('router')

var style = {
  link: {
    color: 'inherit'
  }
}

module.exports = Link({
  className: 'link',
  style: style.link,
  navigate: router.navigate.bind(router)
})
