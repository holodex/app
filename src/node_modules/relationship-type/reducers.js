'use strict';

var initialState = {
  ids: ['is-member-of', 'stewards'],
  byId: {
    'is-member-of': {
      name: 'is-member-of',
      labels: ['{source} belongs to {target}', '{source} is part of {target}'],
      color: '#3F51B5'
    },
    'stewards': {
      name: 'stewards',
      labels: ['{source} stewards {target}'],
      color: '#F44336'
    }
  }
}

function relationshipTypes(state, action) {
  if (state === undefined) state = initialState;
}

module.exports = {
  initialState: initialState
};
