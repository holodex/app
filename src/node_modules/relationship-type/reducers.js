import testState from 'state'

function relationshipTypeReducer (state = testState, action) {
  if (typeof state === 'undefined') {
    return testState;
  }

  return state;
}

export default relationshipTypeReducer
