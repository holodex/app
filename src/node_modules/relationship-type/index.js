'use strict';

const flux = require('flux')

flux.registerStores({
  relationshipTypes: require('./stores/relationship-types')
})

module.exports = {
  vocab: require('./vocab'),
  actions: require('./actions'),
  getters: require('./getters')
}
