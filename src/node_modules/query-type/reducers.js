import testState from 'state'

function queryTypeReducer (state = testState, action) {
  if (typeof state === 'undefined') {
    return testState
  }

  return state
}

export default queryTypeReducer
