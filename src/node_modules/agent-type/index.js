'use strict';

const flux = require('flux')

flux.registerStores({
  agentTypes: require('./stores/agent-types')
})

module.exports = {
  vocab: require('./vocab'),
  actions: require('./actions'),
  getters: require('./getters')
}
