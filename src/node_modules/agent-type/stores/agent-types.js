'use strict';

const Nuclear = require('nuclear-js')
const Store = Nuclear.Store
const toImmutable = Nuclear.toImmutable

module.exports = Store({
  getInitialState: function () {
    return toImmutable({})
  },

  initialize: function () {

  }
})
