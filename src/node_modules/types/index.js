

const debug = require('debug')('holodex:types')

import agentTypes from 'agent-type'
import agents from 'agent'
import relationshipTypes from 'relationship-type'
import relationships from 'relationship'
import queryTypes from 'query-type'
import queries from 'query'


console.log('agentTypes', agentTypes)

export default {
  agents,
  agentTypes,
  relationshipTypes,
  relationships,
  queryTypes,
  queries
}
