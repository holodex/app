var debug = require('debug')("types");

// describe types
var types = {
  Agent: require('agent'),
  Person: require('person'),
  Group: require('group'),
  RoleType: require('role-type'),
  Role: require('role'),
  RelationshipType: require('relationship-type'),
  Relationship: require('relationship'),
};

debug(types);

module.exports = types;
