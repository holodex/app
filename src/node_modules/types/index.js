

const debug = require('debug')('holodex:types')

const agentTypes = require('agent-type')
const agents = require('agent')
const relationshipTypes = require('relationship-type')
const relationships = require('relationship')
const queryTypes = require('query-type')
const queries = require('query')


console.log('agentTypes', agentTypes)

module.exports = {
  agents,
  agentTypes,
  relationshipTypes,
  relationships,
  queryTypes,
  queries
}
