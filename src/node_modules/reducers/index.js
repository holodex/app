const agents = require('./agents')


module.exports = function (state = {}, action) {
  return {
    agents: agents(state.agents, action),
    agentTypes: agentTypes(state.agentTypes, action),
    relationships: relationships(state.relationships, action),
    relationshipTypes: relationshipTypes(state.relationshipTypes, action)
  }
}
