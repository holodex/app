var Styles = require('react-style');

var vars = require('ui/vars');

module.exports = Styles.create({
  projectSource: {
    position: "fixed",
    top: vars.gutterWidth / 2,
    right: vars.gutterWidth / 2,
  },
})
