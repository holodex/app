var vars = require('ui/vars')

module.exports = {
  projectSource: {
    position: "relative",
    top: vars.gutterWidth / 2,
    right: vars.gutterWidth / 2,
  },
  toggleIcon: {
    position: "relative",
    top: vars.gutterWidth / 4,
    right: 0

  }
}
