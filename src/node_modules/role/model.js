var Model = require('base/model')
var debug = require('debug')('holodex:role:model')

module.exports = Model.extend({
  typeName: 'Role',
  pluralName: 'roles',

  props: {
    type: 'link',
    agent: 'link',
    relationship: 'link'
  },
})
