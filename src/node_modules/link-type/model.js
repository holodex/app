// var debug = require('debug')('holodex:link-type:model')

var Model = require('base/model')

module.exports = Model.extend({
  typeName: 'LinkType',
  collectionName: 'linkTypes',

  props: {
    // id:
    color: 'string',
    relationshipType: 'link',
    source: 'string',
    target: 'string',
    labels: 'string',
    queryLabels: 'string'
  }
})
