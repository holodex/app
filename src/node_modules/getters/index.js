'use strict';
const { createStructuredSelector, createSelector} = require('reselect')
import {
  getNodes,
  getQueries,
  getGroups,
  getContextAgentId } from 'graph/getters'

const debug = require('debug')('getters')

const types = require('types')

export const getProps = createStructuredSelector({
  nodes: getNodes,
  queries: getQueries,
  groups: getGroups,
  contextAgentId: getContextAgentId
})
