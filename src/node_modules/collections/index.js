var debug = require('debug')("collections");

// describe collections
var collections = {
  people: require('person'),
  groups: require('group'),
  relationshipTypes: require('relationship-type'),
  relationships: require('relationship'),
};

debug(collections);

module.exports = collections;
