{
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "format": "uuid"
    },
    "source": {
      "type": "string",
      "format": "uuid"
    },
    "target": {
      "type": "string",
      "format": "uuid"
    }
  },
  "required": ["type", "source", "target"]
}
