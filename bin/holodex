#!/usr/bin/env node

var spawn = require('child_process').spawn
var join = require('path').join

var holodex = spawn(
  'npm',
  ['run', 'start'],
  {
    cwd: join(__dirname, '..'),
    stdio: 'inherit'
  }
)
.on('exit', function(code) {
  process.exit(code)
})
